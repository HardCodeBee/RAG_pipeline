[pytest]
testpaths = tests
norecursedirs = artifacts outputs .git __pycache__
markers =
    faiss_required: requires the faiss-cpu runtime
    full_backend: requires the full experiment dependency set
    slow: may load local models or process the full corpus
    network: requires external network access
